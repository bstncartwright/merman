---
"@kitlangton/merman": minor
---

Add `createMermaidMarkdownRenderer` for rendering fenced Mermaid blocks inside OpenTUI `MarkdownRenderable` content and a `./tui` entrypoint for OpenCode's optional code-block renderer plugin API.
