---
"@kitlangton/merman": patch
---

Fix terminal-width rendering for wide and combined Unicode labels, correct reverse-direction and multiline state transition layout, and size sequence fragment frames for their widest branch label. Clarify that the current OpenTUI-backed package entrypoint requires Bun and constrain the supported OpenTUI peer range to tested versions.
